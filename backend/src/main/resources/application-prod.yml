# No fallback here: without JWT_SECRET in the environment the application refuses to start.
payflow:
  jwt:
    secret: ${JWT_SECRET}

# API docs stay public on purpose: this instance is a portfolio demo.
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
