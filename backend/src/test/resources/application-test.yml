spring:
  datasource:
    url: jdbc:h2:mem:payflow-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;LOCK_TIMEOUT=10000;DB_CLOSE_DELAY=-1
    username: sa
    password:

payflow:
  jwt:
    secret: test-secret-test-secret-test-secret-0123
  demo:
    enabled: false
